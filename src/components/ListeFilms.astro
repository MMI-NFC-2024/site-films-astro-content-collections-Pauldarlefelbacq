---
import type { CollectionEntry } from "astro:content";
import LinkFilm from "./LinkFilm.astro";

interface Props {
    films: CollectionEntry<"films">[];
}
const { films } = Astro.props;
---
{
    films.map((film) => <LinkFilm film={ film } />)
}