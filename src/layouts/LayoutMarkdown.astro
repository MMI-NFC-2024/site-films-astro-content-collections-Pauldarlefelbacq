---
import Layout from "./Layout.astro";

const { frontmatter } = Astro.props;
---
<Layout titre= {frontmatter.titre}>
    <h1 class="text-3xl font-bold">{frontmatter.titre}</h1>
    <div class="prose">
        <slot />
    </div>
</Layout>